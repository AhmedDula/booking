import { NightsBetweenPipe } from './nights-between-pipe';

describe('NightsBetweenPipe', () => {
  it('create an instance', () => {
    const pipe = new NightsBetweenPipe();
    expect(pipe).toBeTruthy();
  });
});
